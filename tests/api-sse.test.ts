import { describe, it } from 'vitest';

// Placeholder: SSE tests are skipped for now because they require streaming clients.
// Implement when we add an SSE test harness or small client helper.

describe.skip('API SSE', () => {
  it('placeholder - should receive progress events', () => {});
});
